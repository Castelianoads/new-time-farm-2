const TILE_SIZE = 16 // Tamanho do mapa em px

export const CONFIG = {
  TILE_SIZE: TILE_SIZE,
  GAME_WIDTH: 10 * TILE_SIZE,
  GAME_HEIGHT: 29 * TILE_SIZE,
  GAME_SCALE: 1.5,
  DEBUG_COLLISION: false
}
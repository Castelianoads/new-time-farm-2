const TILE_SIZE = 16 // Tamanho do mapa em px

export const CONFIG = {
  TILE_SIZE: TILE_SIZE,
  GAME_WIDTH: 29 * TILE_SIZE,
  GAME_HEIGHT: 25 * TILE_SIZE,
  GAME_SCALE: 1.8,
  DEBUG_COLLISION: false
}